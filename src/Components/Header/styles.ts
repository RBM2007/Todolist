import styled from "styled-components";

export const HeaderTodoL = styled.header`
display: flex;
flex: 1;
align-items: center;
justify-content:center;

padding:5rem 0.625rem;
background-color:${props => props.theme['gray-700']};
`